# Copy this file to .env and replace the password before starting the app.
# .env is ignored by Git; this example file contains no real credentials.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=hyrule_compendium
DB_USER=hyrule_app
DB_PASSWORD=replace-with-a-long-random-password

PORT=3000
